'use strict';

angular.module('valora')
  .controller('EvaluationCtrl', ['$scope', '$location', 'mvNotifier', EvaluationCtrl]);

function EvaluationCtrl($scope, $location, mvNotifier) {
  $scope.loadCourse = function (course) {

  }
}